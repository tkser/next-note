import LogoutPage from "@/app/_components/pages/LogoutPage";

export const metadata = {
  title: "Logout | Note",
};

const Login = async () => {
  return (
    <LogoutPage />
  )
};

export default Login;
