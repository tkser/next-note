import AuthWrapper from "@/app/components/AuthWrapper";

const Page = async () => {
  return (
    <AuthWrapper>
      <div>
        <h1>Dashboard</h1>
      </div>
    </AuthWrapper>
  );
};

export default Page;
