import AuthWrapper from "@/app/_components/AuthWrapper";

const Page = async () => {
  return (
    <AuthWrapper>
      <div>
        <h1>Dashboard</h1>
      </div>
    </AuthWrapper>
  );
};

export default Page;
